specVersion: "v1alpha"
serviceId: "mech-prep-app-db"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "mechprepappdb"
      cloudSql:
        instanceId: "mech-prep-app-db-instance"
connectorDirs: ["./connector"]
